apiVersion: v1
kind: Service
metadata:
  name: model-anywhere-svc
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: model-anywhere
  ports:
    - port: 80
      targetPort: {{ .Values.containerPort }}
